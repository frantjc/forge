name: Test
description: |
  Test action
author: frantjc
runs:
  using: docker
  image: Dockerfile
  entrypoint: sh
  args:
    - -c
    - |
      docker ps
    # TODO: docker run -v $GITHUB_WORKSPACE:/tmp alpine ls /tmp
