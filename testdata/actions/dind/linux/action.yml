name: Test
description: |
  Test action
author: frantjc
runs:
  using: docker
  image: docker://alpine:3.19
  entrypoint: sh
  args:
    - -c
    - |
      ls
      ls ..
      docker run --rm -v $GITHUB_WORKSPACE:/tmp alpine ls /tmp
