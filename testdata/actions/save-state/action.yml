name: Save State
description: |
  Save State action
author: frantjc
runs:
  using: composite
  steps:
    - shell: bash
      run: echo hello=there >> $GITHUB_STATE
    - shell: bash
      run: echo "::save-state name=general::kenobi"
    - shell: bash
      run: env
