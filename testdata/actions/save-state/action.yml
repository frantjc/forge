name: Save State
description: |
  Save State action
author: frantjc
runs:
  using: composite
  steps:
    - run: echo hello=there >> $GITHUB_STATE
    - run: echo "::save-state name=general::kenobi"
    - run: env
